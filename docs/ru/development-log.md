# Лог Продвижения Разработки

> Полная хронологическая история разработки ComputeChain с самого начала

---

## 25 декабря 2025 - Исправление межпроцессного EventBus

### Финальное исправление системы SSE событий ✅

**Проблема:** События EventBus не распространялись между процессами (validator ↔ tx_generator).

**Выявленные первопричины:**
1. EventBus импортировался внутри функций → множественные экземпляры
2. Отсутствовал вызов `setup_event_bridge()` при старте ноды
3. SSE события не работали из-за несериализуемых объектов Transaction в данных событий
4. Mempool не эмитировал `tx_failed` при истечении TTL

**Решения:**
- Перенесены все импорты EventBus на уровень модуля (паттерн singleton)
- Добавлен вызов `setup_event_bridge(chain)` в `node_cli.py`
- Исправлена JSON-сериализация SSE фильтрацией данных событий
- Добавлен `event_bus.emit('tx_failed')` в `mempool.cleanup_expired()`

**Измененные файлы:**
- `blockchain/rpc/api.py` - Импорт на уровне модуля, фильтрация JSON в коллбэках
- `blockchain/core/chain.py` - Импорт на уровне модуля, логирование ID EventBus
- `blockchain/core/mempool.py` - Импорт на уровне модуля, эмиссия tx_failed
- `blockchain/cli/node_cli.py` - вызов setup_event_bridge()

**Результаты:**
- ✅ EventBus singleton проверен (одинаковый ID во всех модулях)
- ✅ SSE события работают корректно (tx_confirmed, tx_failed, block_created)
- ✅ NonceManager получает события tx_failed
- ✅ Просроченные транзакции корректно очищаются

**Коммит:** `fix EventBus cross-process events: module-level imports, SSE JSON serialization, tx_failed emission on TTL expiry`

---

## 23 декабря 2025 - Фаза 1.4: TX TTL и отслеживание событий

### Система Time-To-Live (TTL) для транзакций ✅

**Реализация:**
- Транзакции истекают через 1 час в mempool
- Автоматическая очистка через `mempool.cleanup_expired()`
- TTL отслеживается для каждой транзакции с временными метками
- Просроченные транзакции помечаются в хранилище квитанций

**Система отслеживания событий:**
- SSE (Server-Sent Events) endpoint: `/events/stream`
- Доставка событий блокчейна в реальном времени
- Типы событий: `tx_confirmed`, `tx_failed`, `block_created`
- HTTP SSE клиент для межпроцессного взаимодействия

**Тестирование и документация:**
- Созданы комплексные тестовые скрипты
- Обновлена вся документация с текущими функциями
- Добавлен анализ производительности в ROADMAP.md
- Создан TEST_GUIDE.md с инструкциями по нагрузочному тестированию

**Результаты тестирования производительности:**
- Низкая нагрузка (1-5 TPS): ✅ Стабильно 12+ часов, 122,778 TX подтверждено
- Высокая нагрузка (100-500 TPS): ❌ Провал - архитектурное ограничение
- Измеренный устойчивый TPS: ~10 TPS
- Определены цели масштабируемости: 100 TPS → 300 TPS → 1000+ TPS

**Коммит:** `Phase 1.4: TX TTL auto-cleanup, event tracking, tests, docs update`

---

## 23 декабря 2025 - Исправление десинхронизации NonceManager

### Критическая ошибка управления nonce 🐛

**Проблема:** Переполнение mempool после ~1000 блоков из-за десинхронизации nonce.

**Симптомы:**
- tx_generator никогда не вызывал `on_tx_confirmed()` когда транзакции включались в блоки
- NonceManager держал ВСЕ ожидающие транзакции в очереди вечно
- `pending_nonce` рассчитывался некорректно из устаревших ожидающих транзакций
- Новые транзакции создавались с неправильными nonce → отклонялись mempool
- Mempool заполнялся до предела (1100 макс)
- Блоки становились пустыми

**Первопричина:**
`_force_sync()` удалял только ожидающие tx с `nonce < blockchain_nonce`, но оставлял транзакции с `nonce >= blockchain_nonce` даже если они уже обработаны или отклонены.

**Исправление:**
- Агрессивная очистка: при изменении `blockchain_nonce` очищать ВСЕ ожидающие транзакции
- Сброс `pending_nonce = blockchain_nonce` для обеспечения синхронизации с состоянием блокчейна
- Уменьшены таймауты для быстрого восстановления (sync: 30s→10s, tx: 120s→30s)

**Результаты:**
- ✅ Больше нет десинхронизации nonce
- ✅ Mempool остается здоровым
- ✅ Непрерывная обработка транзакций
- ✅ Исправление подтверждено в тестировании

**Коммит:** `fix NonceManager nonce desync issue causing mempool overflow`

---

## 22 декабря 2025 - Очистка тестовой инфраструктуры

### Исправления тестовых скриптов 🔧

**Проблемы:**
- `cleanup.sh`: не удалял скрытые директории `.validator_*`
- `start_test.sh`: некорректный флаг `--validator` в команде init
- Старые валидаторы загружали поврежденное состояние блокчейна
- Застрявший proposer и переполнение mempool

**Исправления:**
- Исправлен `rm -rf data/*` для корректного удаления dotfiles
- Исправлен синтаксис инициализации валидатора
- Очищены устаревшие тестовые скрипты
- Улучшена обработка ошибок

**Коммиты:**
- `fix cleanup and initialization bugs in test scripts`
- `refactor testing infrastructure and fix metrics`

---

## 22 декабря 2025 - Метрики и мониторинг

### Исправление метрик Prometheus 📊

**Проблема:** Дублирование экземпляров Prometheus вызывало неправильные метрики.

**Первопричина:** Несогласованность импортов создавала множественные экземпляры метрик.

**Исправления:**
- Исправлены шаблоны импортов для метрик Prometheus
- Обеспечен единственный экземпляр метрик во всех модулях
- Улучшено форматирование Grafana (конверсия wei → CPC, десятичные знаки)
- Добавлены метрики подсчета транзакций
- Добавлена метрика `accounts_total`

**Улучшения инфраструктуры:**
- Создан `cleanup.sh` - унифицированный скрипт очистки
- Создан `start_test.sh` - комплексный запускатель тестов (режимы low/medium/high)
- Удалены устаревшие скрипты: `test_24h.sh`, `run_validators.sh`, `full_test.sh`
- Удалены старые скрипты запуска: `start_node_a.sh`, `start_node_b.sh`
- **Итого очистка:** -706 строк устаревшего кода удалено

**Коммит:** `fix Prometheus metrics (import inconsistency causing duplicate instances) and improve Grafana formatting`

---

## 19 декабря 2025 - Исправление гонки nonce

### Реализация NonceManager 🔄

**Проблема:** Гонки nonce при параллельной генерации транзакций.

**Решение:** Реализован класс NonceManager для отслеживания ожидающих транзакций.

**Функции:**
- Потокобезопасное выделение nonce
- Отслеживание ожидающих транзакций
- Автоматическое восстановление nonce при подтверждении
- Обработка таймаутов для застрявших транзакций

**Коммит:** `resolve nonce race condition with pending transaction tracking (NonceManager class)`

---

## 18 декабря 2025 - Исправления скриптов

**Коммит:** `fix scripts`

---

## 17 декабря 2025 - Фаза 1.3: Инфраструктура

### Снимки состояния и протокол обновления 💾

**Снимки состояния:**
- Автоматическое создание снимков блокчейна
- Возможность быстрой синхронизации (<5 мин)
- Поддержка сжатия (gzip)
- Верификация SHA256 для целостности
- Отслеживание метаданных снимков

**Протокол обновления:**
- Система семантического версионирования
- Фреймворк миграции состояния
- Проверки совместимости версий
- Плавные пути обновления

**CLI/RPC эндпоинты:**
- `cpc-cli snapshot create` - Ручное создание снимка
- `cpc-cli snapshot list` - Список доступных снимков
- `GET /snapshot/latest` - Скачать последний снимок
- `GET /snapshot/{height}` - Скачать конкретный снимок

**Коммит:** `implement Phase 1.3 Infrastructure - State Snapshots, Upgrade Protocol, CLI/RPC endpoints`

---

## 17 декабря 2025 - Завершение Фазы 1.2

### Экономическая модель v2.0 💰

**Централизованная конфигурация:**
- Все параметры сети в `protocol/config/params.py`
- Простое обновление параметров
- Конфиги для разных сетей (devnet, testnet, mainnet)

**Экономическое отслеживание:**
- Отслеживание сожжения/чеканки для управления эмиссией
- Поддержка казны для протокольной ликвидности
- ZK-расчет весов майнеров (подготовка к PoC)

**Система комиссий:**
- Валидаторы устанавливают комиссию (0-20%)
- Автоматическое распределение комиссии при вознаграждениях за блок
- Вознаграждения делегаторов пропорциональны стейку

**Коммит:** `implement Economic Model v2.0 - centralized config, ZK-based miner weights, burn/mint tracking, treasury support (Phase 1.2)`

### Реализация периода разблокировки 🔒

**Проблема:** Мгновенное отзыв делегирования позволяет экономические атаки.

**Решение:** 21-дневный период разблокировки для делегирований.

**Функции:**
- Очередь разблокировки с автоматической обработкой
- Токены заблокированы в период разблокировки
- API/CLI для запросов статуса разблокировки
- Пропорциональное распределение вознаграждений во время разблокировки

**Параметры:**
- Период разблокировки: 21 день (181,440 блоков @ 10s на mainnet)
- Devnet: 100 блоков для тестирования

**Эндпоинты:**
- `GET /delegator/{address}/unbonding` - Запрос статуса разблокировки
- `cpc-cli query unbonding {address}` - CLI запрос

**Коммит:** `feat: implement unbonding period (Phase 1.2) - 21-day delegation lock, auto queue processing, API/CLI for unbonding queries`

**Коммит завершения:** `Phase 1.2 FULLY COMPLETED`

---

## 17 декабря 2025 - Фаза 1.1: Полное делегирование

### Пропорциональное распределение вознаграждений 💸

**Реализация:**
- Индивидуальное отслеживание делегирований на делегатора
- Пропорциональное распределение вознаграждений на основе стейка
- Вычет комиссии перед вознаграждениями делегаторов
- Отслеживание истории вознаграждений

**Функции:**
- Модель `Delegation` с индивидуальным отслеживанием
- Расчет вознаграждения: `(delegator_stake / total_stake) × (reward × (1 - commission))`
- Комиссия валидатору: `reward × commission`
- API эндпоинт истории вознаграждений

**Тестирование:**
- Перенесены все тесты в директорию `tests/`
- Добавлен удобный скрипт `run_tests.sh`
- 24 комплексных теста проходит ✅

**Эндпоинты:**
- `GET /delegator/{address}/rewards` - Запрос истории вознаграждений
- `GET /validator/{address}/delegations` - Список делегирований

**Коммиты:**
- `feat: add delegator reward distribution and consolidate tests`
- `feat: complete Phase 1.1 delegation system`

---

## 14 декабря 2025 - Документация и концепция

### Обновления документации 📚

**Обновления:**
- Удалены упоминания стадий из README
- Добавлен дисклеймер о тестовой сети
- Создан `CHANGELOG_SINCE_RESTRUCTURE.md` с полной историей разработки
- Удалена старая директория docs/ (перенесена в отдельный проект документации)
- Обновлены `QUICK_START.md` и `VALIDATOR_PERFORMANCE_GUIDE.md`

**Концептуальная документация:**
- Добавлен `CONCEPT.md` - видение проекта высокого уровня
- Объясняет модель Proof-of-Compute
- Описывает архитектуру сети GPU-вычислений
- Очерчивает механизмы marketplace и верификации

**E2E тестирование:**
- Добавлен end-to-end тест для новых функций валидаторов
- Комплексное тестовое покрытие

**Коммиты:**
- `docs: update README and documentation cleanup`
- `add CONCEPT.md`

---

## 12 декабря 2025 - Фаза 1-3: Экосистема валидаторов

### Комплексная система валидаторов 🎯

**Фаза 1: Метаданные валидаторов**

- Добавлены поля метаданных: имя, веб-сайт, описание
- Тип транзакции `UPDATE_VALIDATOR` (30k gas)
- Ставка комиссии для вознаграждений делегирования (0-20%, по умолчанию 10%)
- Dashboard показывает имена валидаторов

**Фаза 2: Система делегирования**

- Транзакция `DELEGATE` (35k gas) - делегировать токены валидатору
- Транзакция `UNDELEGATE` (35k gas) - отозвать делегированные токены
- Отслеживание общего делегированного стейка (`total_delegated`)
- Разделение собственного стейка и делегированного
- Распределение вознаграждений на основе комиссии

**Фаза 3: Продвинутый слэшинг и восстановление**

**Градуированный слэшинг:**
- 1-е заключение: 5% слэш стейка (базовый штраф)
- 2-е заключение: 10% слэш стейка (двойной штраф)
- 3+ заключение: 100% слэш стейка (постоянное исключение)
- Прогрессивные штрафы препятствуют повторным нарушениям

**Механизм досрочного освобождения:**
- Транзакция `UNJAIL` (50k gas + 1000 CPC комиссия)
- Валидаторы могут заплатить для досрочного выхода из заключения
- Сбрасывает счетчик `missed_blocks`
- Немедленно реактивирует валидатора

**Фильтр минимального uptime:**
- Исправлен баг: параметр был определен (75%), но никогда не использовался
- Теперь фильтрует кандидатов с uptime < 75% во время переходов эпох
- Предотвращает вход низкопроизводительных валидаторов в активный набор

**CLI команды:**
```bash
cpc-cli tx update-validator --name "MyPool" --commission 0.15 --from validator
cpc-cli tx delegate cpcvalcons1... 100 --from delegator
cpc-cli tx undelegate cpcvalcons1... 50 --from delegator
cpc-cli tx unjail --from validator
```

**Тестирование:**
- `test_update_validator_metadata` ✅
- `test_delegate_undelegate_flow` ✅
- `test_unjail_transaction` ✅
- `test_graduated_slashing` ✅
- `test_min_uptime_score_filter` ✅

**Улучшения Dashboard:**
- Показывает имя валидатора или сокращенный адрес
- Отображает общий делегированный стейк
- Показывает процент комиссии
- Улучшенный макет с выделением имен валидаторов

**Затраты газа:**
- UPDATE_VALIDATOR: 30,000
- DELEGATE: 35,000
- UNDELEGATE: 35,000
- UNJAIL: 50,000

**Параметры сети:**
- `unjail_fee`: 1000 CPC
- `min_delegation`: 100 CPC
- `max_commission_rate`: 20%

**Будущая работа:**
- [ ] Индивидуальное отслеживание делегирований (пропорциональные награды)
- [ ] Период разблокировки для отзыва делегирования
- [ ] Слэшинг валидаторов за двойное подписание
- [ ] Предложения по управлению для изменения параметров

**Коммит:** `feat: comprehensive validator system improvements (Phase 1-3)`

---

## 12 декабря 2025 - Механизм UNSTAKE

### Вывод стейка валидатором 💰

**Критическая проблема решена:** Валидаторы теперь могут выводить свой стейк.

**Реализация:**
- Добавлена обработка транзакций `UNSTAKE`
- Деактивация валидатора когда power достигает 0
- 10% штраф за unstake в заключении (сожжение)
- Валидация баланса: UNSTAKE требует только комиссию, не сумму

**Обработка транзакций:**
- `TxType.UNSTAKE` добавлен в `GAS_PER_TYPE` (40,000 gas)
- Сумма unstake изымается из `validator.power`
- Сумма возвращается на баланс пользователя
- Штраф применяется если в заключении: `amount × 0.9` возвращается, `amount × 0.1` сжигается

**CLI:**
- Добавлена команда `cpc-cli tx unstake <amount> --from <key>`

**Тестирование (5 новых тестов):**
- `test_stake_unstake_flow` ✅
- `test_unstake_nonexistent_validator` ✅
- `test_unstake_insufficient_stake` ✅
- `test_unstake_full_deactivates_validator` ✅
- `test_unstake_with_penalty_when_jailed` ✅
- **Всего:** 18 blockchain тестов проходит ✅

**Качество кода:**
- Созданы недостающие файлы `__init__.py` для корректных импортов модулей
- Добавлен `DEVELOPMENT_LOG.md` для отслеживания прогресса реализации

**Управление балансом:**
- Только комиссия транзакции вычитается из баланса пользователя
- Сумма unstake изымается из `validator.power` и возвращается пользователю

**Жизненный цикл валидатора:**
- Валидаторы автоматически деактивируются когда `power` достигает 0
- Можно снова застейкать для реактивации

**Коммит:** `feat: implement UNSTAKE mechanism for validators`

---

## 11 декабря 2025 - Фаза 0: Производительность и слэшинг валидаторов

### Автоматизированная система производительности валидаторов ⚡

**Основные функции:**

**Отслеживание производительности:**
- Отслеживание предложенных vs ожидаемых блоков для каждого валидатора
- Мониторинг последовательных пропущенных блоков
- Запись последней активности
- Расчет оценки uptime

**Автоматический механизм заключения:**
- Валидаторы заключаются после 10 последовательных пропущенных блоков
- Длительность заключения: 100 блоков
- Автоматическое удаление из активного набора при заключении
- Предотвращает блокировку слотов консенсуса неактивными валидаторами

**Оценка на основе производительности:**
```
performance_score =
  60% × uptime_score +
  20% × stake_ratio +
  20% × (1 - penalty_ratio)
```

**Умные переходы эпох:**
- Выбор активного набора на основе performance_score (не только стейк)
- Неактивные валидаторы автоматически удаляются
- Новые валидаторы могут присоединиться при достаточном стейке и производительности
- Минимальное требование uptime: 75%

**Штрафы слэшинга:**
- 5% слэш стейка за заключение
- Постоянное исключение после 3 заключений
- Слэшнутый стейк сжигается (удаляется из эмиссии)

**Основные изменения:**
- Расширена модель `Validator` метриками производительности:
  - `blocks_proposed`, `blocks_expected`
  - `missed_blocks_sequential`
  - `uptime_score`, `performance_score`
  - `last_seen_height`
  - `jailed_until_height`, `jail_count`
  - `total_slashed`

- Отслеживание производительности при обработке блоков:
  - Увеличение `blocks_proposed` для proposer блока
  - Увеличение `blocks_expected` для всех активных валидаторов
  - Обнаружение пропущенных блоков и обновление `missed_blocks_sequential`

- Логика заключения:
  - Если `missed_blocks_sequential >= 10`: заключить валидатора
  - Применить 5% штраф слэшинга
  - Установить `jailed_until_height = current_height + 100`
  - Удалить из активного набора

- Улучшения перехода эпох:
  - Расчет performance_score для всех валидаторов
  - Фильтрация по `jailed_until_height`, `min_uptime_score`
  - Выбор топ N по performance_score (не только по стейку)

**Новые API эндпоинты:**
- `GET /validator/{address}/performance` - Детальные метрики производительности
- `GET /validators/leaderboard` - Сортировка по performance_score
- `GET /validators/jailed` - Текущие заключенные валидаторы

**Dashboard и мониторинг:**
- Веб-панель в реальном времени на `http://localhost:8000/`
- Автообновление каждые 10 секунд
- Визуальные индикаторы производительности (цветные оценки)
- Таблица лидеров активных валидаторов
- Секция заключенных валидаторов с обратным отсчетом

**Инструменты тестирования:**
- `start_node_a.sh` - Запуск первичной ноды
- `start_node_b.sh` - Запуск вторичной ноды с автостейкингом
- `open_dashboard.sh` - Открыть dashboard в браузере
- Комплексные руководства и документация по тестированию

**Решенная проблема:**
Ранее оффлайн валидаторы оставались в активном наборе и блокировали слоты консенсуса. Теперь неактивные валидаторы автоматически заключаются и удаляются из ротации, обеспечивая здоровье сети.

**Коммит:** `feat: implement Validator Performance & Slashing System (Phase 0)`

---

## 10 декабря 2025 - Исправление структуры проекта

### Восстановление функциональности после реструктуризации 🔧

**Проблема:** Проект сломан после реорганизации структуры.

**Исправление:** Восстановлена вся функциональность и исправлены пути импортов.

**Коммит:** `fix: restore functionality after project structure reorganization`

---

## 28 ноября 2025 - Фундамент проекта

### Реструктуризация репозитория 📁

**Изменение:** Упрощение структуры репозитория для лучшей организации.

**Улучшения:**
- Более четкая иерархия модулей
- Упрощенные импорты
- Лучшая организация кода

**Коммит:** `Restructure: flatten repository structure, update README`

### Обновления документации 📝

**Изменения:**
- Минималистичная версия README
- Удалены ссылки на docs (перенесены в отдельный проект)
- Обновлен .gitignore

**Коммиты:**
- `Update README: minimalistic version, remove docs references, update .gitignore`
- `update readme`

### Первый коммит 🎉

**ComputeChain Core & CLI - Genesis**

**Ядро блокчейна:**
- Структура блоков с Merkle корнями
- Типы транзакций: TRANSFER, STAKE, SUBMIT_RESULT
- Модель на основе аккаунтов (как Ethereum)
- Система подписей ECDSA (secp256k1)
- Упорядочивание транзакций на основе nonce
- Модель газа для защиты от спама

**Консенсус:**
- Multi-validator PoA (Proof-of-Authority)
- Round-robin производство блоков
- Детерминированный выбор proposer
- Ротация валидаторов на основе эпох

**Сеть:**
- P2P сеть с обнаружением пиров
- Распространение блоков
- Трансляция транзакций
- Управление пирами

**Управление состоянием:**
- Состояние аккаунтов с балансами
- Реестр валидаторов
- Расчет корня состояния
- Постоянство состояния

**CLI кошелек:**
- Управление ключами (создание, импорт, список)
- Подписание и трансляция транзакций
- Запросы баланса
- Операции валидаторов

**RPC API:**
- RESTful HTTP API
- Запросы состояния блокчейна
- Отправка транзакций
- Запросы валидаторов

**Тестирование:**
- Юнит-тесты основной функциональности
- Тесты валидации транзакций
- Тесты управления состоянием

**Архитектура:**
```
blockchain/      # L1 нода (консенсус, состояние, сеть)
protocol/        # Определения протокола (типы, крипто, конфиг)
cli/             # CLI кошелек (cpc-cli)
tests/           # Юнит-тесты
```

**Начальные функции:**
- ✅ Создание и валидация блоков
- ✅ Обработка транзакций
- ✅ Управление состоянием аккаунтов
- ✅ Стейкинг валидаторов
- ✅ Мульти-валидаторный консенсус
- ✅ P2P сеть
- ✅ CLI кошелек
- ✅ RPC API
- ✅ Модель газа
- ✅ Верификация подписей

**Коммит:** `Initial commit: ComputeChain Core & CLI`

---

## Статистика разработки

**Всего коммитов:** 27
**Период разработки:** 28 ноября 2025 - 25 декабря 2025 (28 дней)
**Строк кода:** ~15,000+ (активная кодовая база)
**Тестов:** 25+ юнит-тестов (все проходят ✅)

**Основные вехи:**
- ✅ Genesis - Ядро блокчейна & CLI
- ✅ Фаза 0 - Производительность и слэшинг валидаторов
- ✅ Фаза 1 - Метаданные валидаторов и делегирование
- ✅ Фаза 2 - Вознаграждения на основе комиссии
- ✅ Фаза 3 - Градуированный слэшинг и unjail
- ✅ Фаза 1.1 - Пропорциональные вознаграждения делегирования
- ✅ Фаза 1.2 - Период разблокировки и экономическая модель
- ✅ Фаза 1.3 - Снимки состояния и обновления
- ✅ Фаза 1.4 - TX TTL и отслеживание событий

**Текущий статус:**
- Устойчивый TPS: ~10 TPS
- Время блока: 10 секунд
- Валидаторы: До 5 (настраиваемо)
- Финальность: Мгновенная (Tendermint BFT)
- Тестовое покрытие: Комплексное

**Следующие цели:**
- Фаза 1.4.1: 100 TPS (блоки 5s, параллельная валидация)
- Фаза 1.4.2: 300 TPS (блоки 3s, кеширование состояния)
- Фаза 1.4.3: 1000+ TPS (Layer 2, шардинг)

---

**Последнее обновление:** 25 декабря 2025
**Участники:** Команда ComputeChain Core
